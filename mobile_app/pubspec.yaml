name: mobile_app
description: "OFS Manager App"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^16.0.0
  flutter_appauth: ^9.0.1
  http: ^1.4.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.4
  uuid: ^4.5.1
  jwt_decoder: ^2.0.1
  skeletonizer: ^2.1.0+1
  backend_client:
    path: backend_client

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
